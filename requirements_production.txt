# Production Requirements for GerdsenAI MLX Manager
# Core dependencies
flask==3.1.0
flask-cors==4.0.0
flask-socketio==5.3.6
python-socketio==5.11.0
websockets==12.0
requests==2.31.0
psutil==5.9.6
numpy
scipy

# Apple Silicon and ML dependencies
# Note: These should be installed via conda or pip on macOS
# mlx>=0.0.6  # Apple MLX framework (macOS only)
# coremltools>=7.0  # Core ML tools (macOS only)
llama-cpp-python>=0.2.0  # GGUF inference engine (with Metal support on macOS)
onnx>=1.15.0  # Open Neural Network Exchange format
onnxruntime>=1.16.3  # ONNX Runtime for inference
# For Apple Silicon: pip install onnxruntime-silicon

# Data handling and serialization
pydantic
dataclasses-json==0.6.3
typing-extensions==4.8.0

# Async and concurrency
asyncio-mqtt==0.16.1
aiofiles==23.2.1

# Monitoring and metrics
prometheus-client==0.19.0
py-cpuinfo==9.0.0

# Security and authentication
cryptography
pyjwt==2.8.0
bcrypt==4.1.2

# Configuration and environment
python-dotenv==1.0.0
pyyaml==6.0.1
toml==0.10.2

# Logging and debugging
structlog==23.2.0
colorlog==6.8.0

# Production WSGI servers
gunicorn==21.2.0  # Unix/Linux production server
waitress==2.1.2  # Cross-platform production server

# File handling and compression
pathlib2==2.3.7
zipfile36==0.1.3

# Network and HTTP
httpx==0.25.2
aiohttp==3.9.1

# MCP Tools and Workspace Management
pyppeteer==1.0.2  # Puppeteer for web testing/debugging
GitPython==3.1.40  # Git repository detection for workspace identification
sqlite3  # Built-in SQLite support for workspace databases
beautifulsoup4==4.12.2  # HTML parsing for web scraping if needed
selenium==4.15.2  # Alternative web automation (optional)

# API integrations
brave-search-api  # Brave Search API integration (if package exists)
# Note: May need to use requests directly for Brave Search API

# Development and testing (optional)
pytest==7.4.3
pytest-asyncio==0.21.1
black==23.11.0
flake8==6.1.0
mypy==1.7.1

# macOS specific (install manually if needed)
# pyobjc-core>=10.0  # For macOS system integration
# pyobjc-framework-Cocoa>=10.0  # For macOS UI integration
# pyobjc-framework-Metal>=10.0  # For Metal framework access
# pyobjc-framework-CoreML>=10.0  # For Core ML integration

# Optional: For enhanced system monitoring on macOS
# py-applescript>=1.0.3  # For AppleScript integration
# osascript>=0.1.0  # Alternative AppleScript interface
