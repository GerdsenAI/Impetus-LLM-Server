# Core dependencies for Impetus-LLM-Server
fastapi==0.92.0
uvicorn==0.20.0
pydantic==1.10.5
flask-sqlalchemy==3.1.1

# Additional dependencies for AI model management
torch==2.6.0
numpy==1.26.4

# Dependencies for Apple Silicon optimization
# Note: Ensure torch is built for Apple Silicon (M1/M2) compatibility

# Development and testing dependencies
pytest==7.2.1
black==23.1.0
isort==5.12.0
