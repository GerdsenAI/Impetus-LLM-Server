# Impetus LLM Server Requirements (Python 3.13 Compatible)
flask>=3.1.0
flask-cors>=4.0.0
flask-socketio>=5.6.0
psutil>=7.0.0
requests>=2.32.0

# Logging and validation
loguru>=0.7.3
pydantic>=2.12.0
pydantic-settings>=2.13.0

# macOS menu bar app (macOS only)
rumps>=0.4.0; sys_platform == "darwin"
pyobjc-core>=11.1; sys_platform == "darwin"
pyobjc-framework-Cocoa>=11.1; sys_platform == "darwin"

# HuggingFace and ML dependencies
transformers>=4.52.1
huggingface-hub>=0.34.0
sentencepiece>=0.2.1

# Production server
gunicorn>=22.0.0
eventlet>=0.40.4

# MLX dependencies (for Apple Silicon)
mlx>=0.31.0; sys_platform == "darwin" and platform_machine == "arm64"
mlx-lm>=0.30.7; sys_platform == "darwin" and platform_machine == "arm64"

# Core ML embedding support (optional — install via: pip install impetus-llm-server[ane])
coremltools>=9.0; sys_platform == "darwin"
numpy>=2.3.0

# Vector store for RAG pipeline
chromadb>=1.5.0,<2.0.0
